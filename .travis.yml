language: node_js

node_js:
  - 'node'

install:
  - npm ci

script:
  - npm test --silent
  - npm run build

notifications:
  email:
    on_failure: change

cache:
  directories:
    - ~/.npm # cache npm's cache
    - ~/.cache
